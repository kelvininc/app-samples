{
  "type": "object",
  "title": "Stream IO Configuration",
  "properties": {
    "topic": {
      "type": "string",
      "title": "Kafka Topic",
      "description": "Topic this stream's data is produced to; supports {asset}/{stream} placeholders. A stream without a topic is not exported."
    },
    "priority": {
      "type": "string",
      "title": "Priority",
      "description": "Higher-priority streams are uploaded first whenever a backlog builds up. Streams without a priority rank as Medium; Low drains after unconfigured streams and is discarded first when the buffer cap evicts.",
      "default": "2",
      "oneOf": [
        { "const": "1", "title": "High" },
        { "const": "2", "title": "Medium" },
        { "const": "3", "title": "Low" }
      ]
    }
  },
  "required": ["topic"]
}
