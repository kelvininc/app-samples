{
  "title": "Teams Message",
  "description": "Payload schema for the Teams Message custom action.",
  "type": "object",
  "properties": {
    "channel": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "title": "Channel",
      "description": "Channel to post to; must match a channel configured in the exporter's webhooks"
    },
    "title": {
      "type": [
        "string",
        "null"
      ],
      "title": "Title",
      "description": "Optional card title shown above the message"
    },
    "message": {
      "type": "object",
      "title": "Message",
      "description": "The message to post to the channel.",
      "properties": {
        "text": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "title": "Text",
          "description": "Message text to post to the channel"
        }
      },
      "required": [
        "text"
      ]
    }
  },
  "required": [
    "channel",
    "message"
  ],
  "additionalProperties": false
}
